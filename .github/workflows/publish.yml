name: Publish to NPM

on:
  workflow_run:
    workflows: ["Reversion"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          registry-url: 'https://registry.npmjs.org'
          scope: '@dev-dae'
          always-auth: true 
          token: ${{ secrets.NPM_TOKEN }}
      - name: Install dependencies
        run: npm ci
           
      # - name: Build package
      #   run: npm run build

      - name: Publish to npm
        run: | 
          npm publish --registry=https://registry.npmjs.org/ --token=${{sercets.NPM_TOKEN}} --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }} 
